import React, { ReactElement } from 'react'
import "./Contact.scss";

interface Props {
    
}

export default function Contact({}: Props): ReactElement {
    return (
        <div>
            This is a contact
        </div>
    )
}
