import React, { ReactElement } from "react";

interface Props {}

// To handle all things payments related
export default function Payments({}: Props): ReactElement {
  return (
    <div>
      <h1>hello from the payments screen</h1>
    </div>
  );
}
